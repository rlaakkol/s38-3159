# Specification

* Header magic
* Header endianness
* Header flags field

# Implementation
## pubsub.jsonish
* `null` <-> `None`
* `true` <-> `True`
* `false` <-> `False`
* string `"` quotes
* string `\` escapes

## pubsub.server
* log sensor data
* log client messages (XXX: publish only?)

## pubsub.client
* log sensor data from publish messages

