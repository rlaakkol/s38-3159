# Specification

* Header magic
* Header endianness
* Header flags field

# Implementation
## pubsub.jsonish
* `null` <-> `None`
* `true` <-> `True`
* `false` <-> `False`

## pubsub.server
* log sensor data
* log client messages (XXX: publish only?)

## pubsub.client
* log sensor data from publish messages

